# Client Environment Variables

# Backend API base URL
# Leave blank during local development to use Vite's built-in proxy (requests to /api route to http://localhost:5000)
# In production, set to your deployed backend API URL (e.g., https://api.peejaydavid.dev)
VITE_API_URL=
